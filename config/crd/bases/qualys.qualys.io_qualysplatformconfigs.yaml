---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: qualysplatformconfigs.qualys.qualys.io
spec:
  group: qualys.qualys.io
  names:
    kind: QualysPlatformConfig
    listKind: QualysPlatformConfigList
    plural: qualysplatformconfigs
    singular: qualysplatformconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.platform.serverUri
      name: Server
      type: string
    - jsonPath: .spec.credentials.sourceType
      name: Source
      type: string
    - jsonPath: .status.conditions[?(@.type=="CredentialsReady")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          QualysPlatformConfig is the Schema for the qualysplatformconfigs API.
          It provides cluster-wide Qualys platform configuration including credentials.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QualysPlatformConfigSpec defines the desired state of QualysPlatformConfig.
            properties:
              credentials:
                description: Credentials contains credential source configuration
                properties:
                  externalSecretRef:
                    description: |-
                      ExternalSecretRef references an External Secrets configuration
                      Required when sourceType is "externalSecret"
                    properties:
                      keyMappings:
                        properties:
                          activationId:
                            type: string
                          customerId:
                            type: string
                        required:
                        - activationId
                        - customerId
                        type: object
                      name:
                        type: string
                      namespace:
                        type: string
                      secretStoreRef:
                        properties:
                          kind:
                            default: ClusterSecretStore
                            enum:
                            - SecretStore
                            - ClusterSecretStore
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - keyMappings
                    - name
                    - namespace
                    - secretStoreRef
                    type: object
                  secretRef:
                    description: |-
                      SecretRef references a Kubernetes Secret containing credentials
                      Required when sourceType is "secret"
                      The Secret must contain ACTIVATION_ID and CUSTOMER_ID keys
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  sourceType:
                    allOf:
                    - enum:
                      - secret
                      - externalSecret
                    - enum:
                      - secret
                      - externalSecret
                    description: SourceType specifies the credential source type
                    type: string
                required:
                - sourceType
                type: object
              platform:
                description: Platform contains Qualys platform connection settings
                properties:
                  proxy:
                    description: Proxy contains optional proxy configuration
                    properties:
                      caCertBundle:
                        type: string
                      httpsProxy:
                        type: string
                      proxyFailOpen:
                        type: boolean
                      proxyOrder:
                        enum:
                        - sequential
                        - random
                        type: string
                      qualysHttpsProxy:
                        type: string
                    type: object
                  serverUri:
                    description: |-
                      ServerUri is the Qualys platform URL
                      Example: https://qagpublic.qg2.apps.qualys.com/CloudAgent/
                    pattern: ^https://.*
                    type: string
                required:
                - serverUri
                type: object
            required:
            - credentials
            - platform
            type: object
          status:
            description: QualysPlatformConfigStatus defines the observed state of
              QualysPlatformConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              credentialSource:
                description: CredentialSource describes the resolved credential source
                type: string
              lastValidated:
                description: LastValidated is the timestamp of the last successful
                  credential validation
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
